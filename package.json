{
  "name": "cloud-based-secure-file-storage-analytics-system",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:start": "npm --prefix frontend start",
    "start": "node local-dev-server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongodb": "^6.9.0"
  }
}
